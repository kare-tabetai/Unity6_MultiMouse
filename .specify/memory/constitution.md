<!-- 
シンク・インパクト・レポート (v1.0.0 → v1.1.0)
=============================================
バージョン: 1.1.0（マイナー版バンプ）
批准日: 2025-11-13
最終修正日: 2025-11-13
変更内容: 初学者向けプロジェクト目標を明確化し、5 つの核心原則を 6 つに拡張

変更された原則:
  - 原則I: 「シンプル設計」→ 「初学者向けシンプル設計」
    - 「わかりやすさ」を最優先に明記
    - 初学者が実装の「なぜ」「どうやって」を迅速に習得できることを強調
  
  - 原則V: 「明確なドキュメント及び著作権表示」を拡張
    - 「最初の 5 分で動かせる」README 要件を追加
    - コピペ可能なサンプルコード必須化

追加セクション:
  - 原則VI: 「インターフェースシンプル性」（新規）
    - メインメソッドの引数を 0～1 個に限定
    - 初心者が選択肢で混乱しないよう、最小限インターフェース設計を強制
    - 高度な制御は基本機能と分離

技術スタック更新:
  - IDE を「VSCode」から「Visual Studio Code（初学者向けの軽量性を優先）」に詳細化
  - テスト項目にサンプルシーン実行を明記
  - ドキュメント項目を新規追加（README.md + インラインコメント）

削除セクション: なし

テンプレート更新状況:
  - plan-template.md ✅ 不要（汎用テンプレート - 本プロジェクト固有ゲート不要）
  - spec-template.md ✅ 不要（汎用テンプレート - 本プロジェクト固有要件不要）
  - tasks-template.md ✅ 不要（汎用テンプレート - 本プロジェクト固有タスク型不要）

理由: テンプレートは汎用設計で、プロジェクトごとに計画・仕様・タスクが具体化される。
      本憲法は「なぜ」と「原則」を定義し、テンプレートは「どうやって」を定義する立場分離。

後続TODO: なし
-->

# Unity6_MultiMouse 憲法

Windows API を C# DllImport 経由で使用して、Unity 6 で複数マウス入力を処理するサンプルプロジェクト。

## 核心原則

### I. 初学者向けシンプル設計

コードは初学者が読んで即座に理解できるシンプルさを優先しなければならない（MUST）。複雑な実装・高度な設計パターン・抽象化は、正当化が明示的でかつドキュメント化されていない限り禁止される。YAGNI（You Aren't Gonna Need It）の原則を厳密に適用し、複数マウス入力の実証に直接貢献する機能のみを実装する。単一の責務を持つ関数を優先し、インターフェースはさらにシンプルにする。

**根拠**: 初学者向けサンプルの最大の価値は「わかりやすさ」にある。複雑さを排除することで、ユーザーは実装の「なぜ」と「どうやって」を迅速に習得でき、自分のプロジェクトへの応用が容易になる。

### II. Windows API via DllImport（非交渉原則）

Windows API との全ての相互作用は、C# DllImport を用いて Windows ライブラリ（user32.dll など）に直接アクセスしなければならない（MUST）。C++ ラッパー層、COM interop、または第三者の Windows 抽象化ライブラリは一切許可されない。宣言には必ずパラメータ説明と参照元ドキュメントの注釈が必須である。

**根拠**: 直接 DllImport を使用することで、プロジェクトは C# 内で自己完結し、この技法をクリアに実証できる。ユーザーは同じパターンを他の Windows API に適用できる。

### III. 複数マウス対応は必須

システムは複数接続されたマウスからの入力を同時に処理しなければならない（MUST）。入力処理は単一マウスの存在を前提としてはいけない。各マウスはデバイス ID などを通じて独立して追跡・識別可能でなければならない（MUST）。

**根拠**: これはプロジェクトの存在を正当化する核心機能である。全ての設計判断はこの能力の実現を優先すべき。

### IV. 実行時デモンストレーション必須

プロジェクトは複数マウス入力を視覚的に実証する動作する Unity シーンを含まなければならない（MUST）。デモンストレーションは独立して実行可能でなければならない――Unity 6 でシーンを開く以上の複雑なビルドツールやセットアップは不要である（MUST）。

**根拠**: ユーザーは動作する機能を見ることで最も よく学ぶ。ライブでインタラクティブなデモは、ドキュメント単独よりも信頼性がある。

### VI. インターフェースシンプル性（新規原則）

API・メソッド・設定オプションは初学者が即座に使える程度の最小限必須な形でなければならない（MUST）。複数マウスを取得するメインメソッドは、引数が 0～1 個に限定される。複数の使用方法は存在してはいけない。詳細な制御が必要な場合は、基本インターフェースと分離して提供されなければならない。「ただ使うだけ」の ユースケースで、複雑な初期設定が不要でなければならない（MUST）。

**根拠**: 初心者ユーザーは選択肢が多いと混乱する。最初は「動く」ことが最優先である。オプション・設定・カスタマイズは、基本機能を習得した後に段階的に学べるよう分離すべき。

### V. 明確なドキュメント及び著作権表示

全ての DllImport 宣言には Windows API ドキュメントソースを参照する行コメントが必須である（MUST）。自明でない技術的選択には必ず正当化理由をコメント記載する。README には セットアップ手順、API 参照、及びサンプルコード出典の著作権表示を含めなければならない（MUST）。**初学者が「最初の 5 分」で動かせるよう、README には最小限のセットアップ手順と完全なコピペ可能なサンプルコードを含めなければならない。**

**根拠**: サンプルコードは、他者がその実装背景の「なぜ」を理解し、自らのプロジェクトに適用できてこそ価値を持つ。初学者にとって、「簡潔で動く例」と「学べるコメント」が最高の学習教材である。

## 技術スタック（固定）

- **エンジン**: Unity 6
- **言語**: C#
- **IDE/エディタ**: Visual Studio Code（初学者向けの軽量性を優先）
- **プラットフォーム対象**: Windows（デスクトップ）
- **Windows API**: C# DllImport 経由でアクセス（user32.dll など）
- **テスト**: Unity Editor での手動検証＋サンプルシーン実行（初期段階では自動テストフレームワーク不要）
- **ドキュメント**: README.md（セットアップ）＋ インラインコメント（実装の詳細）

このスタックからの逸脱は、実装前に明確な正当化と承認が必須である。

## 開発ワークフロー

### コードスタイル及び構造

- 1ファイル 1 クラス/インターフェース；名前空間の構成はフォルダ構造に対応
- 変数とメソッドは camelCase を使用；クラスは PascalCase を使用
- 全てのパブリックメソッドには XML ドキュメントコメントが必須
- DllImport 宣言は専用ユーティリティまたは API モジュールに集約

### Git ワークフロー

- 機能は機能ブランチで開発（`feature/[説明]` または `[チケット]-[説明]`）
- main へのコミット全てが、実装中の機能またはバグ修正を参照するべき
- プルリクエストには以下を含めなければならない：
  1. 変更の説明
  2. テスト方法（該当する場合）
  3. その変更が複数マウス機能の実証を行う確認

### 変更レビューゲート

全ての変更は以下を検証しなければならない：

1. シンプル設計の原則が保たれている（不正当な複雑性がない）
2. Windows API アプローチが DllImport 標準に適合している
3. 複数マウス機能が退行していない
4. デモシーンが Unity Editor で実行可能な状態である

## ガバナンス

**憲法の権限**: この憲法は全ての開発実践とドキュメントに優先する。矛盾が生じた場合、本ドキュメントが最優先される。

**修正プロセス**:

- 軽微な更新（説明、タイプミス、フォーマット）→ パッチ版バンプ（X.Y.Z → X.Y.+1）
- 新原則の追加または重要なガイダンス拡張 → マイナー版バンプ（X.Y.Z → X.+1.0）
- 原則の削除または重大な再定義 → メジャー版バンプ（X.Y.Z → +1.0.0）
- 全ての修正は LAST_AMENDED_DATE とバージョン番号を更新する必須
- 修正はこのファイル先頭にサマリーコメントを追加する必須

**準拠確認**: オープン PR の週次チェックで 6 つの原則への準拠を検証する。コードレビュー中に見つかった違反はマージ前に解決されることが必須。

**運用ガイダンス**: 日常的な開発質問については、README.md 及びインラインコメント内の運用ガイダンスを参照のこと。本憲法は「なぜ」を定義し；README は「どうするか」を定義する。

---

**バージョン**: 1.1.0 | **批准日**: 2025-11-13 | **最終修正日**: 2025-11-13
